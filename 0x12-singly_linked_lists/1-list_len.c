#include <stdlib.h>
#include "lists.h"

/**
 * list_len - returns the number of elements
 * @h: pointer 
 * Return: elements
 */
size_t list_len(const list_t *x)
{
	size_t x = 0;

	while (x)
	{
		n++;
		x = x->next;
	}
	return (n);
}
